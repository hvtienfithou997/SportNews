@model IEnumerable<SportNews247.Database.User>

@{
    ViewBag.Title = "TaiKhoan";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="table-responsive">
    <table class="table">
        <tr>

            <th>
                Username
            </th>
            <th>
                Tên thành viên
            </th>
            <th>
                Quyền
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.username)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.per)
        </td>
        <td>
            @*@Html.ActionLink("Duyệt tin", "CheckNew", new { id = item.id }, new { @type = "submit", @class = "btn btn-success" })
            @Html.ActionLink("Từ chối", "CheckNew", new { id = item.id}, new { @type = "submit", @class = "btn btn-danger" })*@
            @Html.ActionLink("Xóa tài khoản", "Delete", new { id = item.id })
        </td>
    </tr>
        }
    </table>
</div>